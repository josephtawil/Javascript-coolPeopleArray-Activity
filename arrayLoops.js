var array = ["A string", 5, 'T',["another",5,0], true,false];

//for loop is saying for a variable index that is set to 0
// index is less than array length
//every loop index is added by 1.
for(var index = 0; index < array.length;index++)
{
    // console.log("the index is:", index);
    // console.log("hello");
    console.log(array[index]);
}